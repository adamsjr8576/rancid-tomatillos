import React from 'react';
import {shallow} from 'enzyme';
import MoviesContainer from './MoviesContainer';
 
describe('Movies Containers', () => {
let wrapper;
 
beforeEach(() => {
wrapper = shallow(<MoviesContainer />)
})
 
it.skip('', () => {
})
 
});